"""Sandbox entry point for: {{title}}

Generated by the api-to-sandbox skill. This is the client init + the first
"golden path" call the learner runs. Auth comes from the environment / OAuth2
injection — there is no secret in this file, by design.
"""
import os
import requests

# --- Auth (from env / OAuth2 injection; never hardcoded) ---------------------
{{auth_snippet}}

# --- Golden path: the first call a newcomer should get working ---------------
{{golden_path}}
